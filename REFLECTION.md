

## 1. 哪些 Superpowers 技能发挥了最大作用、哪些"形式大于实质"？

对我这个项目帮助最大的技能是 subagent-driven-development，而最"形式大于实质"的是 brainstorming
的默认产物约定。

18 个 task 里，每个实现都要经过三个上下文互不相通的 agent：实现者、spec合规评审员、代码质量评审员。评审员看不到实现过程，只能对着 SPEC、PLAN和最终代码说话。这个设计最初让我觉得冗余——为什么写完还要两个人各审一遍？直到 Task 4：实现者逐字执行了PLAN，spec 合规一次通过，质量评审却 REJECT 了它，理由是护栏拦截路径不消耗步数、循环可能永不终止。这个bug 的根因在我的 PLAN 里，实现者照计划办事没有任何错误——如果只有实现者自己审视自己的代码，它永远不会发现自己"忠实执行了一个错误的计划"。三个 Critical（护栏循环、搜索ReDoS、打包崩溃）全部是这种"陌生视角"抓到的。

反面的例子是 brainstorming 技能对产物路径的默认约定：它要求 spec 存到docs/superpowers/specs/，而课程交付要求根目录的 SPEC.md。我每个项目都要手工覆盖这个默认值。约定本身没错，但它脱离了我真实的使用场景，就成了流程的仪式感——遵守它对产出没有任何帮助。
所以我现在判断一个技能价值的尺子很简单：它给流程里插入了多少陌生视角。插入陌生视角的技能（subagent评审、冷启动验证）价值极高；只规定产物摆放位置的约定，可能只是形式。


## 2. TDD 强制在 AI 协作下是阻碍还是放大器？

TDD 在 AI 协作下是放大器，但它放大的对象变了——从"实现者的思考"变成了"评审者的证伪能力"。

最直接的证据是 Task 4。实现者逐字执行了 PLAN，测试全部通过，spec 合规评审也一次通过。但质量评审 REJECT了它：护栏拦截的路径不消耗步数预算，如果 LLM 持续产出被拦截的动作，循环永远不会终止。这个 Critical不是 TDD 防住的——测试全绿的时候代码就是错的。TDD 真正做的，是给了修复一个可以证伪的框架：修复之后，评审员写出了三计数器单调有界的终止性证明，还用对抗性实测（连续 200轮拦截交替）验收。没有测试框架，这些验证都无从落地。

但"绿"在 AI 协作下也被贬值了。Task 8 的状态机有 5 个测试全绿，评审员却证明：把 approve() 和 reject()改成两个静态返回值，这 5 个测试照样全绿——因为它们只断言了单次决策的形状，没有断言状态迁移。人类写代码时，绿至少说明你亲手实现过那个逻辑；AI 写代码时，绿可能只是它学会了让测试通过。
所以我的结论是：TDD 的仪式部分（先红、文件不存在ModuleNotFoundError）在 AI协作下确实变成了形式，但它的核心——让每一处修复都有可证伪的验收——比人类时代更重要。TDD的重心从防笔误，转移到了防"计划层面的错误被合规的实现掩盖"。

提交之后我用 DeepSeek 真实跑了一次交互模式，又给这个结论加了一层证据：mock 测试永远测不出真实 LLM 的输出形态。DeepSeek 的回复是"I'll help you create an addition program..."加一段解释文字，然后才是 JSON 动作；写到一半还会在回复里嵌入带花括号的 C 代码块。我的解析器最初只能处理干净 JSON 和 markdown 围栏，这两种形态都导致格式错误。mock 脚本永远输出完美 JSON，所以 154 个测试全部通过也发现不了这个问题——真实 LLM 的容错层只能靠真实调用迭代出来。测试框架能证伪代码，但证伪不了"测试数据之外的世界"。

## 3. subagent-driven 工作流让智能体能自主运行多久而不偏离主题？

从我的经历看，一个 task 量级的任务（二十分钟到一小时）完全在 subagent 的自主能力范围内。18 个 task 跑下来，没有一次因为"做着做着去做别的事"而需要推倒重来；用 Codex 做冷启动验证时，它只读 SPEC 和 PLAN 也顺利完成了整个 task，全程没有偏离。

但真正让我重新理解"偏离"这个词的，是冷启动验证里 Codex 的行为。它没有闷头把 Task 2 做完，而是先停下来问了 5 个问题——其中两个暴露了 PLAN 的真实缺陷（parameters 非对象时该不该拦截、message 键缺失时该怎么报错）。Task 5 的实现者更直接：它没有照抄 PLAN 的测试命令，而是先在 Windows 上实证了 cmd.exe 不剥单引号，发现照抄的命令永远 exit 0，然后修正了它。我原以为"自主运行"意味着不提问，实际上高质量 subagent 的特征恰恰是在正确的位置停下提问。

所以我对"偏离"的结论是：subagent 很少做无关的事，它最危险的偏离是另一种形态，对错误计划的过度忠实。Task 4 的实现者逐字执行了 PLAN 里的护栏计步错误，Task 14 的实现者忠实照抄了与文件不匹配的编辑脚本。它们都没有跑题，它们只是太听话了。防住这类偏离的机制不是更强的约束，而是 prompt 里那句"遇到不确定立即提问"，这句话比任何"请保持专注"都有用，因为它把"偏离"从缺陷变成了纠错通道。

## 4. 什么样的 task 颗粒度最优？

我最初以为 task 的大小可以用文件数量衡量：一个文件是一个小 task，多个文件是一个大 task。护栏 L1 就是"一个文件"——它却经历了三轮评审循环，每一轮评审员都找到新的绕过（套壳命令、白名单逃逸、rm 的等价拼写），前后修了近十处缺陷。而 Docker 分发同样是三个文件的量，一轮就通过了。

对比这两次经历我才明白，task 的真实大小不在文件数，在验收标准的对抗性强度。护栏是安全核心，它的验收标准天然是"攻击者能否绕过"，这是一个没有尽头的标准，每一轮修复都会暴露下一轮变体。Docker 的验收标准是"构建是否成功、容器能否启动"——确定性标准，通过就完了。同一个"文件数量"的 task，工作量可以差五倍。

所以我的颗粒度原则是：在 PLAN 阶段先问一句"这个 task 的验收标准有对抗性吗"。有，就拆小、预排对抗测试轮、给修复轮留出计划内的余量；没有，就可以合并同类任务。按这个标准回看，护栏 L1 其实应该拆成"基础规则"和"对抗加固"两个 task，把军备竞赛从意外变成计划的一部分。

## 5. SPEC / PLAN 质量如何影响实现质量（举一个"规约不清导致 subagent 偏离"的具体案例）？

最典型的一次发生在 Task 14。PLAN 里 fix_bug.json 的编辑脚本写着 old_string 用四空格缩进，而同一个 PLAN 自己给出的 auth.py 样例文件用的是八空格缩进加行尾注释。照抄实现的话，demo 的编辑动作必然匹配失败——修 bug 的演示会当场演砸。实现者没有照抄，它逐字节比对了 PLAN 里的两处代码，发现矛盾后修正了脚本。这个偏离是好的偏离，但它本不该发生。

类似的内部矛盾在 18 个 task 里出现了五次：Task 6 的 vim 在精确匹配表里但测试要求前缀拦截、Task 9 的 pickle.loads 在禁用名单里但匹配逻辑只看裸模块名、Task 17 的掩码格式测试期望与实现代码不一致。这五次有一个共同的模式：测试代码块和实现代码块由同一个人分开起草，从来没有交叉核对过。我原以为 PLAN 的质量等于写得详细，实际它的质量首先取决于内部一致性，一份自相矛盾的计划比一份简略的计划更危险，因为实现者会在两个错误的解释之间随机选择。

而暴露这类问题最有效的机制，恰恰是课程强制的那一步：冷启动验证。换一个只读文档的 agent 来做一两个 task，它会在我未明文写下的每一个假设处撞墙。Codex 在 Task 2 提的五个问题里，两个直接指向 PLAN 的真实缺陷。这一步的成本是一两个小时，收益是让计划在真正开发前经过一次证伪。

## 6. 你最有效的 prompt / context 策略是什么、为什么有效？

我派给实现者的 prompt 有一个固定的五段结构：工作目录与环境约束、现有模块的真实 API 清单、完整的 task 文本（代码块全文粘贴）、明确的完成标准、以及一条"不要运行 git 命令"。最开始这些段落只是为了让 subagent 少来问我，后来才发现其中最有价值的是第二段。

Task 13 是接线任务，要串起前面 12 个 task 的全部模块。如果让实现者自己去读代码，它读到的接口可能已经过时——十几个 task 之间经过多轮评审修复，函数签名变了不止一次。所以我在 prompt 里直接粘贴了当前版本的真实签名清单，并写明"以下 API 以代码为准，勿凭 PLAN 旧代码"。实现者没有在任何一个接口上接错线。

这件事让我得出一个反直觉的结论：给 subagent 提供 context 的最好方式不是告诉它"文件在哪里"，而是把文件里会过期的部分直接复制进 prompt。引用路径会漂移，粘贴签名不会。subagent 的上下文要自包含且不过期，这条原则在我之后每个 task 的派工里都成立。

## 7. 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

Task 12 的配置加载用了一个相对路径的算术：从 loader.py 的位置往上数三层找到仓库根。在开发环境（editable 安装）下，这个算术永远正确，所有测试全绿。直到质量评审员做了一件我当时没想到的事：他真实构建了一个 wheel，解包，在模拟的 site-packages 布局里 import——路径指向了错误的位置，而且 default.yaml 根本没被打进 wheel。修完之后，Task 15 的静态文件又重演了完全相同的错误。

第二次是 Task 17 的凭据存储。docstring 写的是"keyring 优先，环境变量兜底"，但 keyring 在 headless Linux 上，也就是 Render 容器里——没有 Secret Service daemon 时会直接抛异常，兜底永远不会生效。这两件事的共同点不是实现难度，而是验证环境与目标环境的不一致：我在源树里开发测试，交付物却要跑在 wheel 和容器里。"在我机器上能跑"是分发工程的第一谎言。

这条工程要求真正逼我想清楚的是：每个涉及资源定位或凭据的任务，验收标准必须从"在开发环境跑通"改成"从全新环境安装后验证"。评审员用真实 wheel 实测的那次，比我自己跑一百次 pytest 都更有说服力。

## 8. 如果重做你会改变什么？

回看 18 个 task 的评审记录，三个 Critical 分别出现在护栏循环、搜索 ReDoS、打包崩溃，加上三轮军备竞赛和两次打包回归，全部集中在"安全"和"打包"两个主题上。这两个主题之外的任务，基本都是一轮通过。

如果重做，我会在 PLAN 阶段就做一个检查：这个 task 的验收标准有对抗性吗？安全类任务对抗的是恶意输入，打包类任务对抗的是陌生环境。这两类 task 的评审轮次要预排进计划，甚至把"对抗测试轮"写成 PLAN 里的独立 task，而不是等质量评审意外地变成军备竞赛。我当时的做法是写 PLAN、派实现、等评审发现问题再修，三轮护栏竞赛里，前两轮其实是可以预见的。

换句话说，我不会改变工具和工作流的选择，但会改变 PLAN 的写作方式：在写每个 task 之前先问"谁会攻击这个交付物"，把答案写进验收标准。

## 9. 你对 Superpowers 这套方法论的批判——它假设了什么，这些假设在你的项目里成立吗？

Superpowers 的流程里没有冷启动验证。writing-plans 的输出直接进入执行，中间没有一个"换一个 agent 证伪计划"的环节。这一步是课程强制要求、我手工插入的，而它恰恰是暴露 PLAN 内部矛盾最有效的机制。流程假设了计划一旦写完就是可信的，在我的项目里，这个假设五次被证伪。

第二个假设藏在评审循环里：评审员强于实现者。但我的评审员自己就犯过错：Task 6 的评审员建议的 rm 正则，实测是死代码，不匹配任何一个变体，是实现者实证后修正了它。评审是概率性防线，不是绝对防线。它让错误更难漏过去，但不会让错误消失。

所以我的批判是：Superpowers 假设流程纪律可以替代判断力，而本项目最关键的三个 Critical 都是流程之外的能力抓到的——评审员的对抗性思维（把护栏当攻击目标）、目标环境思维（用真实 wheel 实证）、演示诚实性思维（证明测试可以被架空）。流程让平庸的执行不至于失败，判断让优秀的执行有意义。AI 协作工程师的价值不在执行流程，在知道什么时候流程不够。
